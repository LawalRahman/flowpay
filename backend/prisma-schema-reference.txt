// Prisma schema file location: ./prisma/schema.prisma
// This documents the structure for reference

/*
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  password      String
  walletAddress String?
  workflows     Workflow[]
  drips         Drip[]
  transactions  Transaction[]
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model Workflow {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  name      String
  trigger   String
  conditions String[]
  actions   String   // JSON serialized
  active    Boolean  @default(true)
  drips     Drip[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Drip {
  id         String   @id @default(cuid())
  workflowId String
  workflow   Workflow @relation(fields: [workflowId], references: [id])
  userId     String
  user       User     @relation(fields: [userId], references: [id])
  amount     Float
  currency   String   // XLM or USDC
  frequency  String   // daily, weekly, monthly, continuous
  duration   Int
  active     Boolean  @default(true)
  transactions Transaction[]
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model Transaction {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  dripId    String?
  drip      Drip?    @relation(fields: [dripId], references: [id])
  amount    Float
  currency  String   // XLM or USDC
  status    String   // pending, completed, failed
  hash      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
*/
